spring:
  datasource:
    username: root
    password: root
    url: jdbc:mysql://localhost:3306/leco-springcloud-alibaba
  application:
    name: user-provider
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848
server:
  port: 8070
dubbo:
  cloud:
    subscribed-services:
  protocol:
    name: dubbo
    port: -1
